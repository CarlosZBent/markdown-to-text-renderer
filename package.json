{
  "name": "markdown-to-text-renderer",
  "version": "0.1.0",
  "description": "An extension for the marked library to easily convert from markdown format to plaintext",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "marked",
    "markdown",
    "plaintext"
  ],
  "author": "liquidsnake",
  "license": "ISC",
  "dependencies": {
    "marked": "^5.0.4"
  }
}
